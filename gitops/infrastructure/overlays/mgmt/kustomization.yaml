# ============================================================
# mgmt 클러스터 overlay
# base + mgmt 전용 리소스 (Kiali, Grafana Ingress 등)
# ============================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  # mgmt 전용 리소스 추가 시 여기에 선언
  # - monitoring-ingress.yaml
  # - kiali-ingress.yaml
